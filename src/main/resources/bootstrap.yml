server:
  port: 4000

spring:
  cloud:
    config:
      uri: http://localhost:8888
  application:
    name: client
  profiles:
    active: dev

management:
  endpoints:
    web:
      exposure:
        include: "*"